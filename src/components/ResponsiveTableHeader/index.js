import React from 'react'

const ResponsiveTableHeader = ({ children }) => {
    return (
        <div className="responsive-table-row responsive-table-header">
            {children}
        </div>
    )
}

export default ResponsiveTableHeader
